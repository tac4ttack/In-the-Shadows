using UnityEngine;

public class DebugCam : MonoBehaviour
{
    // public float MoveSpeed = 1f;
    // public float MouseSensivity = 1f;

    // public GameObject Earth;

    // void Update()
    // {
    // transform.Translate(Vector3.forward * Input.GetAxis("Vertical") * MoveSpeed * Time.deltaTime);
    // transform.Translate(Vector3.right * Input.GetAxis("Horizontal") * MoveSpeed * Time.deltaTime);
    // transform.Rotate(Vector3.up * Input.GetAxis("Mouse X") * MouseSensivity * Time.deltaTime);
    // transform.Rotate(Vector3.left * Input.GetAxis("Mouse Y") * MouseSensivity * Time.deltaTime);

    // transform.RotateAround(Earth.transform.position, Vector3.up, Input.GetAxis("Mouse X") * MouseSensivity * Time.deltaTime);
    // transform.RotateAround(Earth.transform.position, Vector3.left, Input.GetAxis("Mouse Y") * MouseSensivity * Time.deltaTime);
}
